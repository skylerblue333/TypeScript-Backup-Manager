{
  "name": "@skycoin4444/backup-plan-registry",
  "version": "0.2.0",
  "description": "Bounded in-memory registry for validated backup plans; does not execute backups",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "jest --runInBand",
    "check": "npm run build && npm test && npm audit --omit=dev --audit-level=high"
  },
  "dependencies": {
    "express": "^4.22.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.20",
    "@types/jest": "^29.5.6",
    "@types/supertest": "^2.0.15",
    "jest": "^29.7.0",
    "supertest": "^6.3.3",
    "ts-jest": "^29.1.1",
    "typescript": "^5.9.0"
  }
}
